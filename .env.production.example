# 正式部署環境變數範本
# 用法：複製為 .env（與 docker-compose.fullstack.yml 同目錄），填入實際值後再啟動：
#   cp .env.production.example .env   然後編輯 .env
# ⚠️ .env 內含機密，請勿提交到 git。

# ============ 必改：機密與密碼 ============

# JWT 簽章金鑰（登入權杖）。用以下指令產生長隨機值後貼到等號右邊：
#   openssl rand -base64 48
JWT_SECRET=CHANGE_ME_openssl_rand_base64_48

# 資料庫密碼。用以下指令產生：
#   openssl rand -base64 24
DB_PASSWORD=CHANGE_ME_openssl_rand_base64_24

# 後台 admin 預設密碼為 Demo@1234，請於上線後第一時間更改：
#   以 admin 登入 → 使用者與權限管理 → 對自己帳號按「發重置碼」→ 用該碼設定新強密碼。
#   （種子對既有帳號不覆寫密碼，重啟不會還原。）

# ============ 網域與 HTTPS（本階段保留，未啟用）============
# 之後要啟用正式 HTTPS 時再填：
#   1) 將 DOMAIN 設為你的網域，PUBLIC_ORIGIN 設為 https://該網域
#   2) DNS 以 A 記錄指向本機公開 IP，防火牆開放 80 與 443
#   Caddy 會自動申請 Let's Encrypt 憑證。先不實作可維持 localhost。
DOMAIN=localhost
PUBLIC_ORIGIN=https://localhost
